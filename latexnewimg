#!/bin/bash 

# This script makes new img latex files with cli options

## Constants

RIGHT_NOW="$(date +"%x %r %Z")"
TIME_STAMP="Updated $RIGHT_NOW by $USER"
TYPE="img"
TYPEDIR="$TYPE/"

## Functions

write_file(){
    cat <<- _EOF_
% $TIME_STAMP
\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{./$dir$imgname.$ext}
    \caption[$toc_caption]{$caption}
    \label{fig:$label}
\end{figure}
_EOF_
}

update(){
    echo "\input{./$dir$filename}" >> listofimg.tex 
    echo "\ref{fig:$label}" >> listofreferences.tex
}

usage(){
    echo "usage: $0 [option]..."    
}

help(){
    usage
    echo "Add a new $TYPE .tex file to the LaTeX document."
    echo
    echo "  -o,  --output              output filename         (filename.tex)"
    echo "  -d,  --directory           directory                       (img/)"
    echo "  -c,  --caption             caption text            (\"A cool pic\")"
    echo "  -t,  --toc                 TOC text                (\"A cool pic\")"
    echo "  -l,  --label               reference label             (cool_pic)"
    echo "  -e,  --ext                 image file extension            (.png)"
    echo "  -u,  --update              update listofref.tex and listofimg.tex"
    echo "  -h,  --help                display this help and exit"
    echo
    echo "Visit https://github.com/tdpage/latex_tools for more info"
}

## Main

# initialize some fields
filename=       # filename including extension
imgname=        # 
directory=      # directory to place file
caption=        # caption under the float 
toc_caption=    # caption that appears in TOC
label=          # reference label tag
ext="png"       # file extension if linking to external file
updateFlag=     # add file and ref to listofimg.tex and listofref.tex

# read flags
while [ "$1" != "" ]; do
    case $1 in
        -o | --output    )      shift
                                filename=$1
                                ;;
        -d | --directory )      shift
                                dir=$1
                                ;;
        -c | --caption   )      shift
                                caption=$1
                                ;;
        -t | --toc       )      shift
                                toc_caption=$1
                                ;;
        -l | --label     )      shift
                                label=$1
                                ;;
        -e | --extension )      shift
                                ext=$1
                                ;;
        -u | --update    )      updateFlag=1
                                ;;
        -h | --help      )      help
                                exit
                                ;;
        * )                     usage
                                exit 1
    esac
    shift
done

# if certain fields haven't been entered, set some defaults based on the filename
[[ "$filename" = "" ]]    && filename="new_img.tex"
# [[ "$dir" = "" ]]         && dir=""
[[ "$caption" = "" ]]     && caption=${filename%.*}
[[ "$toc_caption" = "" ]] && toc_caption=${filename%.*}
[[ "$label" = "" ]]       && label=${filename%.*}
[[ "$ext" = "png" ]]      && ext="png"
 

imgname=${filename%.*} # name of image is same as filename without extension
 
# if filetype directory exists, add it to dir
if [[ -d "./$TYPEDIR" ]]; then
    dir="$TYPEDIR""$dir"
fi

# do some work
[[ -d "./$dir" ]] || mkdir -p ./$dir
[[ -f "./$dir$filename" ]] || write_file > ./$dir$filename
[[ "$updateFlag" ]] && update
exit 0


